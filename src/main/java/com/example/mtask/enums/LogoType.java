package com.example.mtask.enums;

/**
 * Enum representing the types of logos used in the application.
 * <p>
 * This enumeration is utilized to categorize logo types
 * for different entities.
 * It is used for generating object names when storing
 * logo files in MinIO or similar storage services.
 */
public enum LogoType {

    PRODUCT,
    CATEGORY
}